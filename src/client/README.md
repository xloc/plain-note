# Notes client

This directory contains the Vue and Pinia browser application. Run project commands from the repository root; see the root README for setup and development instructions.

The Vite development server proxies `/api` to the local Worker on port 8787.
